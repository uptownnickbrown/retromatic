[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && npx tsx src/scripts/migrate.ts && cd .. && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
