[build]
builder = "nixpacks"

[deploy]
preDeployCommand = "node backend/dist/scripts/migrate.js"
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
