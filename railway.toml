[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && npx drizzle-kit push --force && cd .. && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
